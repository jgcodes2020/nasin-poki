# nasin poki for Fcitx5

This is the Fcitx5 table implementation of nasin poki. You can install it like any other CMake project:
```sh
cmake -B build
cmake --build build
sudo cmake --install build
```

The `generator.py` script in this directory regenerates `nasin-poki.json`, the input for the dictionary generator.